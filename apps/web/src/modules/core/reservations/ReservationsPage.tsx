import { ArrowLeft, CalendarDays, CreditCard, QrCode } from 'lucide-react'
import { useEffect, useState } from 'react'
import { apiRequest, apiUrl } from '../../../api/client'
import { dateLabel, reservationStatus } from '../ui/format'
import './reservations.css'
const auth = () => ({ headers: { Authorization: `Bearer ${sessionStorage.getItem('cisne.accessToken') || ''}` } })
export function ReservationsPage({ onBack }: { onBack: () => void }) {
  const [spaces, setSpaces] = useState<any[]>([]), [mine, setMine] = useState<any[]>([]), [selected, setSelected] = useState<any>(), [date, setDate] = useState(''), [startTime, setStartTime] = useState(''), [endTime, setEndTime] = useState(''), [demo, setDemo] = useState(false), [error, setError] = useState(''), [saving, setSaving] = useState(false)
  const load = () => { apiRequest<any[]>('/api/reservable-spaces', auth()).then(setSpaces).catch(e => setError(e.message)); apiRequest<any[]>('/api/member/me/reservations', auth()).then(setMine).catch(e => setError(e.message)) }
  useEffect(load, [])
  const closeSelection = () => { setSelected(undefined); setDate(''); setStartTime(''); setEndTime('') }
  const request = async () => { if (!selected || !date || (!!startTime !== !!endTime)) return setError('Informe a data e, se desejar horário, preencha início e fim.'); setSaving(true); setError(''); try { await apiRequest('/api/member/me/reservations', { method: 'POST', ...auth(), body: JSON.stringify({ spaceId: selected.id, reservationDate: date, startTime: startTime || undefined, endTime: endTime || undefined }) }); closeSelection(); load() } catch (e: any) { setError(e.message) } finally { setSaving(false) } }
  const cancel = async (reservation: any) => { if (!window.confirm('Cancelar esta reserva? O histórico será preservado.')) return; setSaving(true); try { await apiRequest(`/api/member/me/reservations/${reservation.id}/cancel`, { method: 'POST', ...auth() }); load() } catch (e: any) { setError(e.message) } finally { setSaving(false) } }
  return <main className="reservations-page member-reservations-page"><button className="finance-back" onClick={onBack}><ArrowLeft size={18} /> Início</button><span className="eyebrow">Área do Sócio</span><h1>Reservas</h1>{error && <p className="form-error" role="alert">{error}</p>}<p className="reservation-lead">Escolha um espaço, data e horário para solicitar sua reserva.</p><div className="space-grid">{spaces.map(space => <article className="space-card" key={space.id}>{space.imagePath ? <img className="space-card-photo" src={`${apiUrl}${space.imagePath}`} alt={space.name} /> : <div className="space-placeholder"><CalendarDays size={30} /></div>}<h2>{space.name}</h2><p>{space.description || 'Espaço disponível para associados.'}</p><span>{space.capacity ? `Até ${space.capacity} pessoas` : 'Capacidade não informada'}</span><strong>{Number(space.price) ? `R$ ${Number(space.price).toFixed(2).replace('.', ',')}` : 'Gratuito'}</strong><button onClick={() => { setSelected(space); setError('') }}>Ver disponibilidade</button></article>)}</div><section className="my-reservations"><h2>Minhas reservas</h2>{mine.length ? mine.map(r => <article className="reservation-row" key={r.id}><div><strong>{r.space.name}</strong><span>{dateLabel(r.reservationDate)} · {r.startTime && r.endTime ? `${r.startTime} às ${r.endTime}` : 'Dia inteiro'}</span><small>Associado/responsável: você</small></div><b>{Number(r.totalAmount) ? `R$ ${Number(r.totalAmount).toFixed(2).replace('.', ',')}` : 'Gratuito'}</b><em className={r.status}>{reservationStatus[r.status] || 'Não informado'}</em>{r.status === 'APPROVED' && <button onClick={() => setDemo(true)}><CreditCard size={15} /> Pagar reserva</button>}{!['CANCELLED', 'COMPLETED'].includes(r.status) && <button className="btn btn-secondary" disabled={saving} onClick={() => cancel(r)}>Cancelar</button>}</article>) : <p>Nenhuma reserva encontrada.</p>}</section>{selected && <div className="reservation-modal" role="dialog" aria-modal="true"><div>{selected.imagePath && <img className="reservation-selected-photo" src={`${apiUrl}${selected.imagePath}`} alt={selected.name} />}<h2>{selected.name}</h2><p>{selected.description || 'Espaço disponível para associados.'}</p><p>{selected.capacity ? `Capacidade: ${selected.capacity} pessoas` : 'Capacidade não informada'} · Valor: <strong>{Number(selected.price) ? `R$ ${Number(selected.price).toFixed(2).replace('.', ',')}` : 'Gratuito'}</strong></p>{selected.photos?.length > 1 && <div className="reservation-mini-gallery">{selected.photos.map((photo: any) => <img key={photo.id} src={`${apiUrl}${photo.path}`} alt="" />)}</div>}<label>Escolha a data<input type="date" min={new Date().toISOString().slice(0, 10)} value={date} onChange={e => setDate(e.target.value)} /></label><div className="form-two"><label>Horário inicial<input type="time" value={startTime} onChange={e => setStartTime(e.target.value)} /></label><label>Horário final<input type="time" value={endTime} onChange={e => setEndTime(e.target.value)} /></label></div><button className="btn btn-primary" onClick={request} disabled={!date || saving}>{saving ? 'Enviando…' : 'Solicitar reserva'}</button><button className="btn btn-secondary" onClick={closeSelection}>Fechar</button></div></div>}{demo && <div className="reservation-modal" role="dialog" aria-modal="true"><div className="demo-payment"><QrCode size={34} /><h2>Pagamento demonstrativo</h2><p>Nenhuma cobrança real será realizada.</p><div className="demo-qr"><QrCode size={72} /></div><button className="btn btn-secondary" onClick={() => setDemo(false)}>Fechar</button></div></div>}</main>
}
