import { ArrowLeft, Bell, CalendarDays } from 'lucide-react'
import { useEffect,useState } from 'react'
import { apiRequest } from '../../../api/client'
import './community.css'
const h=()=>({headers:{Authorization:`Bearer ${sessionStorage.getItem('cisne.accessToken')||''}`}})
export function AnnouncementsPage({onBack}:{onBack:()=>void}){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{apiRequest<any[]>('/api/member/me/announcements',h()).then(setRows)},[]);return <main className="community-page"><button className="finance-back" onClick={onBack}><ArrowLeft size={18}/> Início</button><span className="eyebrow">Clube Ser Cisne</span><h1>Avisos</h1>{rows.length?rows.map(a=><article className="announcement-card" key={a.id}><Bell size={20}/><div><h2>{a.title}</h2><p>{a.content}</p><small>{new Date(a.startDate).toLocaleDateString('pt-BR')}</small></div></article>):<div className="community-empty">Nenhum aviso no momento.</div>}</main>}
export function EventsPage({onBack}:{onBack:()=>void}){const [rows,setRows]=useState<any[]>([]),[selected,setSelected]=useState<any>();useEffect(()=>{apiRequest<any[]>('/api/member/me/events',h()).then(setRows)},[]);return <main className="community-page"><button className="finance-back" onClick={onBack}><ArrowLeft size={18}/> Início</button><span className="eyebrow">Agenda do clube</span><h1>Eventos</h1><div className="event-grid">{rows.map(e=><article className="event-card" key={e.id}><div className="event-image"><CalendarDays size={32}/><strong>{new Date(e.startDateTime).toLocaleDateString('pt-BR',{day:'2-digit',month:'short'})}</strong></div><h2>{e.title}</h2><p>{e.location||'Clube Ser Cisne'}</p><span>{e.memberPrice?`Sócio · R$ ${Number(e.memberPrice).toFixed(2).replace('.',',')}`:'Entrada demonstrativa'}</span><button onClick={()=>setSelected(e)}>Ver detalhes</button></article>)}</div>{selected&&<div className="community-modal"><div><button className="modal-close" onClick={()=>setSelected(undefined)}>×</button><h2>{selected.title}</h2><p>{selected.description}</p><p><strong>{new Date(selected.startDateTime).toLocaleString('pt-BR')}</strong><br/>{selected.location}</p><button onClick={()=>alert('Inscrições pelo aplicativo serão disponibilizadas em uma próxima etapa.')}>Participar</button></div></div>}</main>}
